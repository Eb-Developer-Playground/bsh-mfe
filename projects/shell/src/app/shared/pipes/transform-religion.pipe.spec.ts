import { TransformReligionPipe } from './transform-religion.pipe';

describe('TransformReligionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformReligionPipe();
    expect(pipe).toBeTruthy();
  });
});
