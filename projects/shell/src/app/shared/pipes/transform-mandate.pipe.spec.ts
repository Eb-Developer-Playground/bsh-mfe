import { TransformMandatePipe } from './transform-mandate.pipe';

describe('TransformMandatePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformMandatePipe();
    expect(pipe).toBeTruthy();
  });
});
