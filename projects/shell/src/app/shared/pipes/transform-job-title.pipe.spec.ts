import { TransformJobTitlePipe } from './transform-job-title.pipe';

describe('TransformJobTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformJobTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
