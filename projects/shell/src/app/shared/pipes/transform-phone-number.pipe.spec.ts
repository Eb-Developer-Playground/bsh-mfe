import { TransformPhoneTypePipe } from './transform-phone-type.pipe';

describe('TransformPhoneTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformPhoneTypePipe();
    expect(pipe).toBeTruthy();
  });
});
