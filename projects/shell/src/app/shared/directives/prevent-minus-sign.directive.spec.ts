import { PreventMinusSignDirective } from './prevent-minus-sign.directive';

describe('PreventMinusSignDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventMinusSignDirective();
    expect(directive).toBeTruthy();
  });
});
