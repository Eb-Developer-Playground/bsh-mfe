import { TransformDispatchModePipe } from './transform-dispatch-mode.pipe';

describe('TransformDispatchModePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDispatchModePipe();
    expect(pipe).toBeTruthy();
  });
});
