import { TransformCountyPipe } from './transform-county.pipe';

describe('TransformCountyPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformCountyPipe();
    expect(pipe).toBeTruthy();
  });
});
