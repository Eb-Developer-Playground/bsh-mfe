import { TransformProfessionPipe } from './transform-profession.pipe';

describe('TransformProfessionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformProfessionPipe();
    expect(pipe).toBeTruthy();
  });
});
