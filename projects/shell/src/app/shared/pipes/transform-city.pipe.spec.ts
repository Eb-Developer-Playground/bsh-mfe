import { TransformCityPipe } from './transform-city.pipe';

describe('TransformCityPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformCityPipe();
    expect(pipe).toBeTruthy();
  });
});
