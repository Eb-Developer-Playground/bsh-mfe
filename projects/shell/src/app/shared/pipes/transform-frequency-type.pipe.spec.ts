import { TransformFrequencyTypePipe } from './transform-frequency-type.pipe';

describe('TransformFrequencyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformFrequencyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
