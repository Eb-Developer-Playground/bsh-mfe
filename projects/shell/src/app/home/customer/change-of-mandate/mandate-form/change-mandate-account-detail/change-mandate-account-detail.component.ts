import { Component } from '@angular/core';
import { COMPAT_IMPORTS } from '../../../../../shared/compat-barrel';
@Component({
  selector: 'app-change-mandate-account-detail',
  standalone: true,
  imports: [COMPAT_IMPORTS],
  template: '',
})
export class ChangeMandateAccountDetailComponent {}
